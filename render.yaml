services:
  - type: web
    name: portfolio-contact-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 3000
      - key: RESEND_API_KEY
        sync: false
      - key: CONTACT_TO
        sync: false
      - key: RESEND_FROM
        sync: false
      - key: ALLOWED_ORIGINS
        sync: false
